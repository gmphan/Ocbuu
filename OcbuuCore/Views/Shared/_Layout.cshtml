<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - OcBuu</title>

    @* 
        I got the Slate theme from https://bootswatch.com/
        and replace code with the bootstrap.css below.
     *@
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    @* sometime the footer won't stay at the bottom because the style.css not working
        when it happens, I will need to comment it out rerun the project then uncomment it again *@
    <link rel="stylesheet" href="~/OcbuuCore.styles.css" asp-append-version="true" />

    @* this link is use for bootstrap icon frorm icons.getbootstrap.com *@
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"/>

    @* this toastr make TempData _Notification partial view look pretty *@
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css"/>
    
    <style>
        .indented-paragraph {
            text-indent: 2em; /* Adjust the indentation size as needed */
        }
    </style>
</head>
<body>
    @* <header> *@
        @{
            string controllerName = ViewContext.RouteData.Values["controller"]?.ToString();
            string actionName = ViewContext.RouteData.Values["action"]?.ToString();
            @* Console.WriteLine($"{controllerName}, {actionName}"); *@
        }

        <nav class="navbar navbar-expand-lg bg-dark fixed-top" data-bs-theme="dark" >
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="Visitor" asp-controller="Home" asp-action="Index">OC BUU</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                    <a class="nav-link @((controllerName == "Home" && actionName == "Index")? "active":"")" asp-area="Visitor" asp-controller="Home" asp-action="Index">Home
                        @* <span class="visually-hidden">(current)</span> *@
                    </a>
                    </li>
                    @* <li class="nav-item">
                    <a class="nav-link" href="#">Features</a>
                    </li> *@
                    <li class="nav-item">
                    <a class="nav-link @((controllerName == "Resume" && actionName == "Index")? "active":"")" asp-area="Visitor" asp-controller="Resume" asp-action="Index">Resume</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" asp-area="Visitor" asp-controller="Home" asp-action="Index">About</a>
                    </li>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" asp-area="Visitor" data-bs-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">Action</a>
                        <a class="dropdown-item" href="#">Another action</a>
                        <a class="dropdown-item" href="#">Something else here</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Separated link</a>
                    </div>
                    </li>
                </ul>
                <form class="d-flex">
                    <input class="form-control me-sm-2" type="search" placeholder="Search">
                    <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                </form>
                </div>
            </div>
            </nav>
    @* </header> *@
    
    <div class="container">
        <div class="row" style="height:65px; width:auto">
            @* this to help push down the main pass the navigation
                if I don't want this, I need to remove fix-top from nav
             *@
        </div>
        <main role="main" class="pb-3">

            @* GP added _Notification partial here, so it can be used by all the pages before they rendered *@
            <partial name="_Notification"/>
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer bg-primary text-muted" data-bs-theme="dark">
        <div class="container text-center">
            2024 by OcBuu <i class="bi bi-balloon-heart-fill"></i>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    @* GP added - this use for rich tex editor *@
    <script src="https://cdn.tiny.cloud/1/wai1xaxr9pab4qyzmjor8aaqbgq463ry2scgcn8438u6pbki/tinymce/6/tinymce.min.js" referrerpolicy="origin"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
